apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "rcm.fullname" . }}
  labels:
    {{- include "rcm.labels" . | nindent 4 }}
spec:
  {{- if not .Values.autoscaling.enabled }}
  replicas: {{ .Values.replicaCount }}
  {{- end }}
  selector:
    matchLabels:
      {{- include "rcm.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      {{- with .Values.podAnnotations }}
      annotations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      labels:
        {{- include "rcm.labels" . | nindent 8 }}
        {{- with .Values.podLabels }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
    spec:
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      serviceAccountName: {{ include "rcm.serviceAccountName" . }}
      securityContext:
        {{- toYaml .Values.podSecurityContext | nindent 8 }}
      containers:
        - name: {{ .Chart.Name }}
          securityContext:
            {{- toYaml .Values.securityContext | nindent 12 }}
          image: {{ printf "%s/%s:%s" .Values.global.azure.images.runtimeClassManager.registry .Values.global.azure.images.runtimeClassManager.image .Values.global.azure.images.runtimeClassManager.tag }}
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          {{- if .Values.rcm.leaderElectEnabled }}
          args:
          - leader-elect
          {{- end }}
          env:
          - name: CONTROLLER_NAMESPACE
            value: {{ .Release.Namespace }}
          - name: SHIM_DOWNLOADER_IMAGE
            value: "{{ printf "%s/%s:%s" .Values.global.azure.images.shimDownloader.registry .Values.global.azure.images.shimDownloader.image .Values.global.azure.images.shimDownloader.tag }}"
          - name: SHIM_NODE_INSTALLER_IMAGE
            value: "{{ printf "%s/%s:%s" .Values.global.azure.images.nodeInstaller.registry .Values.global.azure.images.nodeInstaller.image .Values.global.azure.images.nodeInstaller.tag }}"
          - name: SHIM_NODE_INSTALLER_JOB_TTL
            value: "{{ .Values.rcm.nodeInstallerJob.ttl | default 0 }}"
          ports:
            - name: http
              containerPort: {{ .Values.service.port }}
              protocol: TCP
          livenessProbe:
            {{- toYaml .Values.livenessProbe | nindent 12 }}
          readinessProbe:
            {{- toYaml .Values.readinessProbe | nindent 12 }}
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
          {{- with .Values.volumeMounts }}
          volumeMounts:
            {{- toYaml . | nindent 12 }}
          {{- end }}
      {{- with .Values.volumes }}
      volumes:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
